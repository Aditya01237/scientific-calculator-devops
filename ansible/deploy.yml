- hosts: servers
  tasks:

    - name: Pull docker image
      docker_container:
        name: calculator
        image: adityapareek01/calculator:latest
        state: started
        recreate: yes
        ports:
          - "7070:8080"